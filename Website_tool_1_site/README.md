Simple static website

Files:
- index.html
- styles.css
- script.js

To view locally:

1) Open `index.html` directly in your browser.

or

2) Run a local HTTP server from the `Website_tool_1_site` folder:

```bash
python -m http.server 8000
```

Then open http://localhost:8000
